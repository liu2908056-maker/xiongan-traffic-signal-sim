# 复核记录（2026-08-29）

## 材料完整性

- 程序：Excel 导入转换、场景校验、CityFlow 启动、引擎检查脚本齐全。
- 数据：20 个受控路口、roadnet、拓扑、信号方案、路口清单、早/平/晚高峰流量齐全。
- 压力扰动：`flow_midday_pressure10/20/30/120.json` 均存在且路由引用合法道路。
- 复现依赖：官方 CityFlow 源码及 `pybind11`、`rapidjson` 子模块已随包提供；Dockerfile 固定 Python 3.10。
- 文档与接口：README、地图几何、安装说明、验收记录、在线/离线启动脚本齐全。

## 执行结果

1. `python src/validate_scenario.py`：PASS；20 路口、294 道路，三时段流量与 20/20 Excel 审计一致。
2. Docker 从本包 Dockerfile 重建：PASS；源码编译 CityFlow 0.1，构建阶段校验和引擎导入均通过。
3. 在线镜像运行：早高峰 120 步 PASS；晚高峰 30 步 PASS。
4. 压力扰动：`flow_midday_pressure30.json` 120 步 PASS，调度 1,124 辆、完成估计 1,122 辆。
5. 离线归档：`run_offline.ps1` 自动 `docker load` 后运行 PASS；`docker/SHA256SUMS.txt` 校验匹配。

主办方推荐入口：有网络执行 `docker compose run --rm cityflow-xiong-an-20`；无网络执行 `run_offline.ps1`。使用这两个入口会将 `outputs` 挂载到提交目录。
